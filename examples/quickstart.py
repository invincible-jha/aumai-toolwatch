"""Minimal working example for aumai-toolwatch.

Run this file directly to verify your installation:

    python examples/quickstart.py
"""

# Toolwatch — Detect runtime changes in tool behavior via fingerprinting
# Replace the placeholder below with your first real usage example.


def main() -> None:
    """Entry point for the quickstart example."""
    print("aumai-toolwatch quickstart — replace this with real usage.")


if __name__ == "__main__":
    main()
